#include<stdio.h>
#include<conio.h>
int main ()
{
    // int isSunday = 0;
    // int isSnowing = 1;
    // printf("%d \n", isSunday && isSnowing);
    // return 0;

    int isMonday = 1;
    int isRaining =1;
    printf("%d \n", isMonday || isRaining);
    return 0;
}